server:
  port: 2000

spring:
  application:
    name: LMS
  datasource:
    url: jdbc:mysql://localhost:3306/lms
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  